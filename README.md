# drf_project
## 🚀 Современное API для управления курсами и уроками на базе Django REST Framework: масштабируемо, удобно и технологично.

# О проекте
drf_project — это готовое решение для построения системы дистанционного обучения (LMS), с расширяемой архитектурой и профессиональным подходом к разработке API.
Всё, что нужно для управления онлайн-курсами и уроками — в одном компактном сервисе!

* Технологии: Python 3.12, Django 5.2, DRF, Celery, PostgreSQL, Redis, Docker.

* Готов к быстрому запуску в любом окружении (Windows, Linux, macOS).

* Полная поддержка асинхронных тасков, JWT-авторизации, групп пользователей и расширяемых моделей.

## 💡 Главные возможности
* API для курсов и уроков: полный CRUD, вложенные запросы, удобные сериализаторы.

* Пользовательская модель User: email, телефон, город, аватар, расширенная аутентификация.

* Асинхронные задачи Celery: автоматическая деактивация неактивных пользователей, email-уведомления, поддержка Periodic Tasks.

* Изолированная инфраструктура: быстрый старт через Docker Compose, интеграция с внешними сервисами Stripe.

* Админка, права, JWT-авторизация: рулевые права, ограничения, тестируемость через pytest.

## 🚦 Быстрый старт (Docker Compose)
1. Клонировать репозиторий:

~~~
git clone https://github.com/natali83911/drf_project.git
cd drf_project
~~~

2. Заполнить файл .env:

* Скопируйте .env.example и пропишите конфиг доступа к БД, Redis, секретные ключи.

3. Запустить проект одной командой:

~~~
docker-compose up --build
~~~

4. Готово!

* Откройте http://localhost:8000 и пользуйтесь API.

## 🔍 Проверка сервисов и диагностика
* Django:
Откройте http://localhost:8000.
В логах (docker-compose logs web) — должны быть строки об успешной миграции и запуске.

* Celery Worker:

~~~
docker-compose logs celery_worker
~~~
Ожидаемые сообщения:
celery@... ready. Connected to redis://redis:6379/0

* Celery Beat:

~~~
docker-compose logs celery_beat
~~~
Ожидаемые сообщения:
beat: Starting... DatabaseScheduler: Schedule changed.

* PostgreSQL:
Войти внутрь контейнера:

~~~
docker exec -it postgres_db psql -U postgres -d drf_project
~~~
Проверить пользователей:

~~~
SELECT id, username, email, last_login, is_active FROM auth_user;
~~~

## ⚡ Архитектура и структура
~~~
drf_project/
├── config/           # settings, urls, wsgi/asgi
├── lms/              # API логика курсов и уроков
├── users/            # Модель и API пользователей
├── manage.py
├── requirements.txt
├── .env
└── README.md
~~~

## 🛠️ Использование API
Примеры запросов:

* GET /lms/courses/ — получить список курсов

* POST /lms/courses/ — создать курс

* GET /lms/lessons/ — список уроков

* POST /lms/lessons/ — создать урок

Тестируйте через Postman, curl или встроенный Swagger (drf_spectacular).

## ✉️ Автоматические уведомления о событиях курса
* Фоновая email-рассылка — реализована асинхронная задача через Celery: при любом обновлении курса всем подписанным пользователям автоматически отправляется письмо с описанием изменений.

* Email формируется на лету по шаблону:
** Тема: Обновление курса: <название>
** Текст: "Курс '<название>' был обновлен. Загляните, чтобы узнать подробности."

* Нет дублирования писем или рассылки всем подряд: отправка идет только активным почтовым адресам, зарегистрированным на этот курс!

## 🧪 Тестирование
~~~
pytest
~~~
Все основные компоненты покрыты unit и интеграционными тестами.

## 📦 Зависимости (requirements.txt)
~~~
Django==5.2.6
djangorestframework==3.16.1
django-filter==25.1
djangorestframework_simplejwt==5.5.1
django-extensions==4.1
drf-spectacular==0.28.1
django-celery-beat==2.6.0
celery==5.3.6
eventlet==0.34.1
python-dotenv==1.1.1
psycopg2-binary==2.9.10
pillow==11.3.0
redis==5.0.3
black==25.1.0
pytest-django==4.11.1
coverage==7.10.7
~~~

## 🏆 Преимущества
* Мгновенный старт через Docker.

* Поддержка scaling и продакшн.

* Яркая документация, современная архитектура.

* Чистый код и best practices Python/DRF.

## 📧 Контакты и обратная связь
Вопросы, баги и предложения —  на почту автора в профиле.
